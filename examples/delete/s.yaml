edition: 1.0.0          #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: vpcApp            #  项目名称
access: aliyun-release  #  秘钥别名

services:
  vpc-test: #  服务名称
    component: vpc  # 组件名称
    props:
      # s delete
      regionId: cn-qingdao
      vpcId: xxx
      vSwitchId: xxx
      securityGroupId: xxx


